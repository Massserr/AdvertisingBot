{
  "name": "advertising-bot-platform",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "prisma:generate": "pnpm --filter @adbot/api prisma:generate",
    "prisma:migrate": "pnpm --filter @adbot/api prisma:migrate",
    "db:seed": "pnpm --filter @adbot/api db:seed"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
